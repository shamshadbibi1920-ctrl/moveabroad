# MoveAbroad.pk Environment Configuration
# This client-side application runs as a static SPA without mandatory API keys.

# APP_URL: Canonical base URL for the deployed web application.
APP_URL="https://moveabroad.pk"
